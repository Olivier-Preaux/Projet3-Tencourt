{% extends 'base.html.twig' %}

{% block title %}Contact{{parent()}}
{% endblock %}

{% block body %}
	<h1 class="SendAMessageTitle">Envoyer un message</h1>
	<div class="containerSendEmail">
		<div class="containerMessageForm">
			{{ form_start (form) }}
			<div class="containerMessage">
				<div class='emailFrom'>
					<p class="emailFromTitle">De:</p>
					{{ form_widget(form.from, {'value':  app.user.email  }) }}
				</div>
				<div class='emailTo'>
					<p class="emailToTitle">À:</p>
					{{ form_widget(form.to, {'value':  user.email  }) }}
				</div>
				<div class='emailSubject'>
					<p class="emailSubjectTitle">Sujet:</p> 
					{{ form_widget(form.subject, {'attr': {'placeholder': 'Sujet du Message'}}) }}
				</div>
				<div class='emailText'>
					<p class="emailTextTitle">Message: </p>
					{{ form_widget(form.html, {'attr': {'placeholder': 'Votre Message'}}) }}
				</div>
				<div class="containerBottomSendMessage">
					<a class="SendAMessageSubmitButton" href="/mail"><input class="account-block-next-button" name="" type="submit" value="Envoyer"></a>
					<a class="backButtonSendMessage" href="{{ path('user_show', {'slug': user.slug}) }}">Retour</a>
				</div>
				{{ form_end (form) }}
			</div>
			<div class="blankSendMessagePage"></div>
		</div>
    </div>
	{% endblock %}
